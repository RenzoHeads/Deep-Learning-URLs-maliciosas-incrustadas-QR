<?xml version="1.0" encoding="UTF-8"?>
<!--
  Lint configuration for the QR security detector app.

  - UnsafeOptInUsageError: AGP lint no reconoce la forma corta de Kotlin
    `@OptIn(ExperimentalGetImage::class)` para el marker de CameraX, y
    la forma nominal `@OptIn(markerClass = ...)` no compila en Kotlin
    1.9 (varargs single-element named assignment forbidden). El compilador
    Kotlin si acepta la forma corta y propaga el opt-in correctamente,
    asi que deshabilitamos el check de lint. NO es un bug de correccion:
    es un falso positivo de lint vs Kotlin opt-in syntax.
-->
<lint>
    <issue id="UnsafeOptInUsageError" severity="ignore" />
</lint>
