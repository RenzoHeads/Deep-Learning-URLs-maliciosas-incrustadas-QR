<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Camera permission — required for live QR scanning via CameraX. -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Internet permission — required for Play Asset Delivery (on-demand model download). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Camera hardware is required; the app cannot function without it. -->
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="true" />

    <!-- OpenGL ES 3.1 for TFLite GPU delegate; the app falls back to CPU if absent. -->
    <uses-feature
        android:glEsVersion="0x00030001"
        android:required="false" />

    <application
        android:name=".AppSeguridadQR"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:hardwareAccelerated="true"
        android:enableOnBackInvokedCallback="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:dataExtractionRules="@xml/data_extraction_rules"
        tools:ignore="DataExtractionRules"
        tools:targetApi="35">

        <!--
            Deshabilita el auto-inicializador de WorkManager.
            El WorkManagerInitializer (ContentProvider de androidx.startup)
            se ejecuta ANTES de Application.onCreate() e inicializa
            WorkManager con DefaultWorkerFactory (sin HiltWorkerFactory).
            Eso hace que los @HiltWorker fallen al instanciar.

            Con el auto-init deshabilitado, WorkManager se inicializa
            on-demand la primera vez que se llama WorkManager.getInstance()
            (desde MediadorSincronizacion, despues de AppSeguridadQR.onCreate()),
            usando el Configuration.Provider que proporciona HiltWorkerFactory.

            Los componentes del scheduler (SystemJobService, ConstraintProxy
            receivers) se habilitan automaticamente cuando WorkManager se
            inicializa on-demand.
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup.InitializationProvider"
                tools:node="remove" />
        </provider>

        <!-- MainActivity hosts all Compose screens (camera, result, history). -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.QRSecurityDetector">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
